SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/kitenge
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=your_db_password
# JWT secret for signing tokens (use 32+ chars). Generate with:
#  PowerShell: [guid]::NewGuid().ToString('N') + [guid]::NewGuid().ToString('N')
#  OpenSSL:    openssl rand -hex 32
JWT_SECRET=change-this-to-a-long-random-string
EMAIL_USER=you@example.com
EMAIL_PASS=your_email_app_password
ADMIN_EMAIL=kitengeboraa@gmail.com
ADMIN_NOTIFICATION_EMAIL=kitengeboraa@gmail.com
ADMIN_WHATSAPP=250788883986
SMS_WEBHOOK_URL=
SMS_SENDER=KitengeBora
APP_CORS_ALLOWED_ORIGINS=http://localhost:4000,http://localhost:3000
FILE_UPLOAD_DIR=uploads
ADMIN_DEFAULT_PASSWORD=change-me
# Set true to force-reset the admin password on startup (local/dev only)
APP_ADMIN_FORCE_PASSWORD_RESET=false
# Seed a few sample products when the database is empty (local/dev only)
APP_SEED_SAMPLE_PRODUCTS=false
APP_FRONTEND_URL=http://localhost:3000
